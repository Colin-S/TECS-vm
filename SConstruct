exe = 'vm'
src = ['main.c','file.c']
Program(exe, src, compiler_options='-Wall -g -std=c99 -O0')

testExe = 'test/vmTest'
testSrc = ['test/testMain.c','test/test_file.c']
Program(testExe, testSrc, compiler_options='-Wall -g -std=c99 -O0')
